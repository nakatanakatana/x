apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: vcluster-app-sync
  namespace: app
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: "clusters/vcluster-app"
  prune: true
  timeout: 2m
  kubeConfig:
    secretRef:
      name: vc-vcluster
      key: config
  dependsOn:
    - name: vcluster-app
      namespace: flux-system
