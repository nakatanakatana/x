apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubeblocks
  namespace: kb-system
spec:
  interval: 10m
  timeout: 10m
  chart:
    spec:
      chart: kubeblocks
      version: "1.0.0"
      sourceRef:
        kind: HelmRepository
        name: kubeblocks
  releaseName: kubeblocks
