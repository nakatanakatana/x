apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: pcloud-s3
spec:
  type: ClusterIP
  selector:
    app: rclone-s3-gateway
  ports:
    - name: s3
      port: 8080
      targetPort: s3
      protocol: TCP
