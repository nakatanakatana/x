apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
namespace: monitoring
resources:
  - deployment.yaml
  - rbac.yaml
  - service.yaml
configMapGenerator:
  - name: observability-local-alloy
    namespace: monitoring
    files:
      - config.alloy
