apiVersion: v1
kind: Service
metadata:
  name: nisshi
  namespace: monitoring
  labels:
    app: nisshi
spec:
  type: ClusterIP
  selector:
    app: nisshi
  ports:
    - name: kafka
      port: 9092
      targetPort: kafka
      protocol: TCP
